<%include partials/header.ejs%>

<h2> The Parks I've Seen</h2>
<div>
  <% visited.forEach(function(campground){ %>
    <img style="height: 200px; width: 200px;" src="<%= campground.image %>">
    <p><%=campground.name %></p>
  <%});%>
</div>



<%include partials/footer.ejs%>
